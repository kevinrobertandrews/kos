import { exists } from "./exists/exists";
import { mkdir } from "./mkdir/mkdir";
import { save } from "./save/save";
import { read } from "./read/read";
export { exists, mkdir, save, read };

const module = {
  exists,
  mkdir,
  save,
  read,
};
export default module;
